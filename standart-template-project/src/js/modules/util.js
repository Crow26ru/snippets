/**
* Скрипт для хранения констант, переменных и функций, которые используются в нескольких JavaScript файлах
*/

'use strict';

(function () {
  var ESC_KEY_CODE = 27;

  window.util = {
    ESC_KEY_CODE: ESC_KEY_CODE
  };
})();
